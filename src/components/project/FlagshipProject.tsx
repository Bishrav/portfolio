import { ArrowUpRight, ExternalLink, Github } from "lucide-react";
import type { Project } from "@/data/portfolio";
import ArchitecturePreview from "./ArchitecturePreview";

export default function FlagshipProject({ project }: { project: Project }) { return <article className="flagship-project"><div className="flagship-copy"><div className="project-meta"><span>{project.category}</span><span className="status-pill">{project.status}</span></div><h3>{project.name}</h3><p className="flagship-summary">{project.summary}</p><ul className="signal-list">{project.highlights.slice(0, 3).map((highlight) => <li key={highlight}>{highlight}</li>)}</ul><div className="tag-list">{project.stack.map((technology) => <span className="tag" key={technology}>{technology}</span>)}</div><div className="project-actions"><a href={project.github} target="_blank" rel="noreferrer"><Github size={16} /> GitHub <ArrowUpRight size={14} /></a>{project.live && <a className="action-secondary" href={project.live} target="_blank" rel="noreferrer"><ExternalLink size={16} /> Live demo</a>}<a className="action-secondary" href={project.caseStudy} target="_blank" rel="noreferrer">Architecture <ArrowUpRight size={14} /></a></div></div><ArchitecturePreview type={project.visual} large /></article>; }
