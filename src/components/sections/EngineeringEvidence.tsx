import { verifiedEvidence } from "@/data/portfolio";
import SectionHeading from "@/components/SectionHeading";
export default function EngineeringEvidence() { return <section className="section section-soft"><div className="container evidence-layout"><div><SectionHeading eyebrow="Evidence" title="Proof over posturing." description="Only repository-backed evidence is shown. Performance and retrieval benchmarks will appear when measured and published." /><div className="evidence-list">{verifiedEvidence.map((item) => <article className="evidence-item" key={item.label}><strong>{item.value}</strong><div><h3>{item.label}</h3><p>{item.note}</p></div></article>)}</div></div><div className="benchmark-note"><span className="signal-dot" /><p>Benchmarking in progress</p><span>Cross-project throughput, latency, retrieval, and load-test results are intentionally omitted until they are measured.</span></div></div></section>; }
